# My Todo (Next.js + TypeScript)

Scaffolded with `create-next-app`. This repo will become a Microsoft To Do clone built with:
- Next.js (App Router)
- TypeScript
- Tailwind CSS (added in next commit)
- Prisma + PostgreSQL (later)
- TanStack Query, NextAuth, Socket.IO (later)

## First steps

```
npm install
npm run dev
```
